<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/rounded_list_item_bg"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/ivFruitIcon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp"
        android:contentDescription="Fruit Icon"
        android:src="@drawable/placeholder_fruit"/>

    <TextView
        android:id="@+id/tvFruitNameItem"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Fruit Name"
        android:textSize="18sp"
        android:textColor="#333333"
        android:textStyle="bold"/>

    <Button
        android:id="@+id/btnDeleteItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:background="@drawable/gradient_button_secondary"
        android:textColor="#FFFFFF"
        android:paddingVertical="8dp"
        android:paddingHorizontal="16dp"
        android:textSize="14sp"
        android:stateListAnimator="@null"/>

</LinearLayout>
